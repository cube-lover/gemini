{
    "api_url": {
        "description": "Flow2API 接口地址",
        "type": "string",
        "default": "http://172.17.0.1:8000/v1/chat/completions",
        "hint": "Flow2API 的 OpenAI 兼容接口地址"
    },
    "apikey": {
        "description": "API 密钥",
        "type": "string",
        "hint": "Flow2API 的 API Key，格式如: han1234",
        "default": "",
        "obvious_hint": true
    },
    "model": {
        "description": "选择生成模型",
        "type": "string",
        "default": "imagen-4.0-generate-preview-landscape",
        "hint": "不同模型支持不同的功能和分辨率；也可填自定义模型ID（优先使用 custom_model）",
        "options": [
          "gemini-2.5-flash-image-landscape",
      "gemini-2.5-flash-image-portrait", 
      "gemini-3.0-pro-image-landscape",
      "gemini-3.0-pro-image-portrait",
      "imagen-4.0-generate-preview-landscape",
      "imagen-4.0-generate-preview-portrait"
        ]
    },
    "custom_model": {
        "description": "自定义模型ID（可选）",
        "type": "string",
        "default": "",
        "hint": "填完整模型ID（例如你的私有/转发模型名）。填写后会加入模型列表，并在未显式选择 model 时作为默认。"
    },
    "prompt_list": {
        "description": "自定义提示词列表",
        "type": "list",
        "default": [
            "手办化:将图片转换为精美的手办风格"
        ],
        "hint": "格式：关键词:描述。添加后可使用 /关键词 命令"
    }
}